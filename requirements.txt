pandas==2.2.2
numpy==1.26.4
plotly==5.22.0
dash==2.17.1
dash-bootstrap-components==1.6.0
prophet==1.1.5
gunicorn==22.0.0```
***Note:*** `gunicorn` is a production-grade web server that PaaS providers use to run Python web apps. It's essential for deployment.

**3. `Procfile` (Process File)**
This is a special file, with no extension, that tells the platform what command to execute to start your web application. Create a new file named `Procfile`.

*   It should contain a single line: